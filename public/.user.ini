; PHP upload limits — honored by cPanel PHP-FPM (ea-php).
; Allows adding many/large gallery photos to a property.
upload_max_filesize = 50M
post_max_size = 120M
max_file_uploads = 60
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
